import { commanderCore } from "../../command/common/command-core";
import { optionUtility } from "../../utils/option";

export function vueCli() {
    const { optionCss, optionVueFramework } = commanderCore;
    const { createNone } = optionUtility;
}
